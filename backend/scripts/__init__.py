"""Backend scripts.

Each module under this package is runnable as
`python -m scripts.<name>` and importable as a plain Python module.

Currently shipped:
    - fixup_xiuzhen_npc_agents : Stage 7 P0 one-shot fix that
      re-bootstraps the 6 九洲一号群 NPC Letta agents with full
      `memory_blocks` and registers the optional `minimax` provider.
"""
